@*@model SqlAgentUIRunner.Models.TaskRunnerModel*@

@{
    ViewBag.Title = "Message Bus";
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jqueryui")
    @Scripts.Render("~/bundles/knockout")
    @Scripts.Render("~/bundles/serviceBus")
}

@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title</h1>
            </hgroup>
        </div>
    </section>
}
<h2>Send Message to Bus</h2>
<fieldset>
    <legend>SprocRunner</legend>
    <div class="row">
        <div class="col-md-3">
            @*            @Html.DropDownList("MessagesDdl", new SelectList(Model.Messages), new {@class = "styled-select"})*@
            @**@
            @*            @foreach (var prop in @Model.Properties)*@
            @*            {*@
            @*                @Html.DisplayFor(modelItem => @prop)*@
            @*                @Html.TextBox("asd")*@
            @*            }*@

            @* <select id="messageTypes" data-bind="options: messages, optionsText: 'messageName', optionsValue: 'id',optionsCaption: 'Please Select', value: selectedMessageId"></select>*@
            <select id="messageTypes" data-bind="options: messages, optionsCaption: 'Please Select', value: selectedMessageId"></select>
            <br />
            <br />

            <div data-bind="foreach: messageProperties, visible: messageTypeSelected">
                <span data-bind="text: name"></span>
                <input type="text" data-bind="value: propValue" />
                <br />
            </div>

        </div>
        <div class="col-md-9">
            <button type="submit" id="Select" class="btn btn-default" data-bind="click: sendMessage">Send Message</button>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div id="PropertyListDiv"></div>
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-md-12">
            @Html.ActionLink("Back to List", "Index", "Home")
        </div>
    </div>
</fieldset>